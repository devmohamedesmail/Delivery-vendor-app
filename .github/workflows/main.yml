name: hello-world
on: push
jobs:
   hello-world-job:
    runs-on: ubuntu-latest
    steps:
       - name: checkout code
         uses: actions/checout@v4
       - run: echo "Hello Wordls"   
    
    
